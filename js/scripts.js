var numbers = [];

function elim(prime) {
  for (j = 2; j <= n; j++) {
    return prime * j;
  }
}

//creates array
for (i = 2; i <= n; i++) {
  numbers.push(i);
}

//cycles array
for (var prime = 2; prime <= n; prime++) {
  delete numbers(elim(prime));
}
